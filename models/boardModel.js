const User = require('../schemas/User');
const Board = require('../schemas/Board');
const Simulation = require('../schemas/Simulation');

module.exports = {
  saveSimulation: (user, simulation, board) => {

  },

  getBoards: (user) => {

  },

  getSimulation: (simId) => {

  }
}
