import React from 'react';

const Exit = (props) => (
  <div>
    <h2 className='doc-title'>Exit</h2>
    <img src="http://i.imgur.com/S3IuMFt.png" />
    <p>Exits are the end of all simulations just as Sources are the beginning.</p>
    <p>A simulation must have, at minimum, a single Source and a single Exit.</p>
    <p>Exits are the end-of-the-line in your simulations. They are also the point at which data collected by Entities moving through the simulation is captured.</p>
  </div>
)

export default Exit;
